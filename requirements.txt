Django>=5.1,<7.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
requests>=2.32
django-environ>=0.11
whitenoise>=6.7
# --- Database drivers (install the one your host uses) ---
# PostgreSQL (used in local dev / hosts with Postgres):
psycopg[binary]>=3.2
# MySQL / MariaDB (common on cPanel shared hosting) — pure-Python, no build step:
PyMySQL>=1.1
