Django>=5.1,<7.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
requests>=2.32
django-environ>=0.11
whitenoise>=6.7
# --- Database driver ---
# MySQL / MariaDB (cPanel) — pure-Python, no build step. Dev uses SQLite (no driver needed).
PyMySQL>=1.1
# PostgreSQL instead? Uncomment:
# psycopg[binary]>=3.2
